# ────────────────────────────────────────────────────
# ACEST FIȘIER ESTE IGNORAT
# ────────────────────────────────────────────────────
# Tunnelul folosește TUNNEL_TOKEN (remote-managed).
# Configurația ingress vine din Cloudflare Dashboard.
#
# Dashboard settings necesare:
#   Public hostname: wherewefishin.uk
#   Service:         http://frontend:80
#   Origin Settings:
#     - Connect Timeout: 30s
#     - HTTP Host Header: (lăsat gol)
#     - No TLS Verify: OFF (folosim HTTP nu HTTPS)
# ────────────────────────────────────────────────────
